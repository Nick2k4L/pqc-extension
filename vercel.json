{
    "functions": {
        "server/main.go": {
            "runtime": "@vercel/go"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server/main.go"
        }
    ]
}